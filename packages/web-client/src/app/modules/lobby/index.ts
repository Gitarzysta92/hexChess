export { LobbySharedModule } from './lobby.shared-module';

import { ROOT_PATH as _ROOT_PATH, routes as _routes } from './lobby.routing';
export namespace Lobby {
  export const ROOT_PATH = _ROOT_PATH;
  export const routes = _routes;
}

