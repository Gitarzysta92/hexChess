import { UniqueNicknameValidatorDirective } from './unique-nickname-validator.directive';

describe('UniqueNicknameValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueNicknameValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
