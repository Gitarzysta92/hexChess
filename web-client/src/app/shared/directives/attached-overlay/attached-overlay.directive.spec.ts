import { AttachedOverlayDirective } from './attached-overlay.directive';

describe('AttachedOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new AttachedOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
